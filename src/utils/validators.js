export const required = () => ({
  required: true
})

export const minLength = (length) => ({
  minLength: length
})

export const maxLength = (length) => ({
  maxLength: length
})

export const email = () => ({
  email: true
})
